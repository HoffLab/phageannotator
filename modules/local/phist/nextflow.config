process {
    withName: PHIST {
        ext.args = params.phist_args ? params.phist_args : ""
        publishDir = [
            path: { "${params.outdir}/PhageHost/phist" },
            mode: params.publish_dir_mode,
            pattern: '*.csv',
        ]
    }
}
