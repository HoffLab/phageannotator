process {
    withName: 'ASPERACLI' {
        ext.args = '-QT -l 300m -P33001'
        publishDir = [
            [
                path: { "${params.outdir}/SRADownload/asperacli" },
                mode: params.publish_dir_mode,
                pattern: "*.fastq.gz"
            ],
            [
                path: { "${params.outdir}/SRADownload/asperacli" },
                mode: params.publish_dir_mode,
                pattern: "*.md5"
            ]
        ]
    }
}
