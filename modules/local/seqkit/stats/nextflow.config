process {
    withName: SEQKIT_STATS {
        ext.args = "--all"
        publishDir = [
            path: { "${params.outdir}/Assembly/seqkit/stats" },
            mode: params.publish_dir_mode,
            pattern: '*.tsv',
        ]
    }
}
