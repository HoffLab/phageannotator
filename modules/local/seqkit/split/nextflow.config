process {
    withName: SEQKIT_SPLIT {
        ext.args = [
            params.seqkit_split_chunk_size ? "--by-size ${params.seqkit_split_chunk_size}" : "",
        ].join(' ').trim()
        publishDir = [
            enabled: false
        ]
    }
}
