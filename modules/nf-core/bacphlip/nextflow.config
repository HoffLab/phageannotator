process {
    withName: BACPHLIP {
        ext.args   = [
            "--multi_fasta"
        ].join(' ').trim()
        publishDir = [
            path: { "${params.outdir}/VirusLifestyle/bacphlip" },
            mode: params.publish_dir_mode,
            pattern: '*.bacphlip'
        ]
    }
}