/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running full-size tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a full size pipeline test.

    Use as follows:
        nextflow run HoffLab/phageannotator -profile test_full,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

params {
    config_profile_name        = 'Full test profile'
    config_profile_description = 'Full test dataset to check pipeline function'

    // Limit resources so that this can run on GitHub Actions
    max_cpus   = 4
    max_memory = '15.GB'
    max_time   = '6.h'

    input                       = file("${projectDir}/assets/test-datasets/samplesheets/reads_only.csv")
    remove_intermediate_files   = false

    /*-----------------------------------------
        Read preprocessing
    -----------------------------------------*/
    // preprocessing options (fastp/bowtie2)
    perform_run_merging         = false
    run_fastp                   = false
    fastp_args                  = null
    run_bowtie2_host_removal    = false
    bowtie2_igenomes_host       = "GRCh38"
    bowtie2_custom_host_fasta   = null
    bowtie2_args                = null

    // virus enrichment options (ViromeQC)
    run_viromeqc                = false
    viromeqc_db                 = null
    save_viromeqc_db            = false
    viromeqc_args               = null


}
